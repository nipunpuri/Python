fname = "mbox-short.txt"
fhand = open(fname)

days = dict()
for line in fhand:
    if line.startswith("From"):
        words = line.split()
        day = words[2]
        days[day] = days.get(day , 0) + 1
        