reg = raw_input("Enter the regular expression")
fhand = open("mbox-short.txt")
import re
lst = []
for line in fhand:
    line = line.rstrip()
    item = re.findall( reg, line) ##re.findall returns a string
    lst.append(item)
    print lst